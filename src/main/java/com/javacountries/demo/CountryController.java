package com.javacountries.demo;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class CountryController {
    // /names/all - return names alphabetically

    // /names/start/{letter} - return alphabetically that start with letter

    // /names/size/{number} - eturn the countries alphabetically that have a name equal to or longer than the given length

    // /population/size/{people} - return the countries that have a population equal to or greater than the given populatio

    // /population/min - return the country with the smallest population

    // /population/max - return the country with the largest population
}
